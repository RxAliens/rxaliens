declare module "react-world-flags" {
  import * as React from "react";
  const Flag: React.ComponentType<{ code?: string; className?: string; style?: React.CSSProperties; alt?: string }>;
  export default Flag;
}
