import { NextRequest,NextResponse } from "next/server"; import { db,initDb } from "@/lib/db"; import { getSteamUser,isAdmin } from "@/lib/steam-session";
export async function GET(req:NextRequest){const a=getSteamUser(req);if(!a||!isAdmin(a.id))return NextResponse.json({error:"Yetkisiz"},{status:403});await initDb();const steamId=req.nextUrl.searchParams.get("steamId");if(!steamId)return NextResponse.json({error:"Steam ID gerekli"},{status:400});const [user]=await db`SELECT * FROM users WHERE steam_id=${steamId}`;if(!user)return NextResponse.json({error:"Kullanıcı bulunamadı"},{status:404});const inventory=await db`SELECT p.id purchase_id,p.price_paid,p.purchased_at,m.* FROM purchases p JOIN market_items m ON m.id=p.item_id WHERE p.steam_id=${steamId} ORDER BY p.id DESC`;const transactions=await db`SELECT * FROM coin_transactions WHERE steam_id=${steamId} ORDER BY id DESC LIMIT 100`;return NextResponse.json({user,inventory,transactions});}
